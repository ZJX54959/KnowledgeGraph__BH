# Markdown知识图谱转换指令

【任务】将原始笔记转换为标准格式
【输入】任意格式的课堂/学习笔记
【输出】严格遵循以下规范的Markdown：

1. 结构规范
   - 必须包含且仅包含两个二级标题：
     ## 【核心概念】
     ## 【关联关系】
   - 三级概念用2空格缩进，禁止使用tab或4空格
     - 正确：`  - 子概念`
     - 错误：`    - 子概念`

2. 概念定义
   - 每行以`- `开头，格式：
     `- 概念名 @类型 [属性]`
   - 属性规则：
     - 键=值用空格分隔，例：`学习率=0.01`
     - 含空格的值用双引号，例：`描述="梯度下降速率"`
   - 建议的属性类别（LLM应优先从以下类别中选择，若无匹配则可自行推断）：
     - `描述`：对概念的详细说明。
     - `来源`：概念的来源或出处。
     - `版本`：概念的版本信息。
     - `创建日期`：概念的创建时间。
     - `作者`：概念的创建者。
     - `状态`：概念的当前状态（如：草稿、已发布）。
     - `重要性`：概念的重要性级别（如：高、中、低）。

3. 关系定义
   - 严格格式：`- 源概念 --> 目标概念 : 类型 [属性]`
   - 必须包含关系类型，例：`: 继承`
   - 建议的关系属性类别（LLM应优先从以下类别中选择，若无匹配则可自行推断）：
     - `强度`：关系的强度或关联程度。
     - `方向`：关系的具体方向性描述。
     - `条件`：关系成立的条件。
     - `时间`：关系发生的时间。

4. 禁止项
   - 禁止使用无序列表符号(*/+)
   - 禁止中文标点（：→等）
   - 禁止未标注类型的节点

【正例】
## 【核心概念】
- 神经网络 @模型 描述="模拟生物神经网络的计算模型" 来源="机器学习" 版本="1.0"
  - 激活函数 @组件 类型=Sigmoid 描述="引入非线性，使神经网络能够学习复杂的模式"
- 主概念1 @类型 属性1=值1 属性2="包含空格的值"
  - 子概念1.1 @类型 描述="子概念的详细描述" 来源="父概念"
    - 孙概念1.1.1 @类型 重要性=高
## 【关联关系】
- 神经网络 --> 激活函数 : 包含
- 梯度下降 --> 随机梯度下降 : 类型=变体 强度=0.9
- 梯度下降 --> 学习率 : 类型=依赖
- 梯度下降 --> 学习率 : 依赖 时间="训练阶段"

【反例】
## 核心概念（错误：缺少【】）
* 神经网络（错误：缺少@类型，使用*）
   - Sigmoid : 激活函数（错误：符号和顺序颠倒）

请直接给出转换后的结果，不要给出任何解释，不需要任何多余的字符。内容务必详实详尽，不要遗漏任何信息。请逐行逐词地对输入文件进行转换。
如果输入内容混乱、无法构成词句、格式奇怪以至于无法理解，请忽视这些并什么都不要输出；但如果可以看出单词间的关系，可以结合上下文与自己的理解进行输出
---